import { Button } from "@material-ui/core";

const Confirm = () => {
    return(
        <div>
            <Button variant="contained">Confirm</Button>
        </div>
    )
};

export default Confirm;